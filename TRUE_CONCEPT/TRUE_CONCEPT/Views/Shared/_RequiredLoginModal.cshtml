
<div class="modal" id="loginModal">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="loginModalLabel">Cảnh báo</h5>
                <button type="button" class="close_Modal" data-dismiss="modal" aria-label="Close" style="background:none; border:none;font-size:25px;font-weight:600">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <p>Bạn cần đăng nhập để thực hiện chức năng này.</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="close_Modal btn btn-secondary" data-dismiss="modal">Đóng</button>
                <a href="/Authentication/Login/Index" class="btn btn-primary">Đăng nhập</a>
            </div>
        </div>
    </div>
</div>

<style>
    .modal {
        display: none;
        position: fixed;
        z-index: 1;
        left: 0;
        top: 0;
        width: 100%;
        height: 100%;
        overflow: auto;
        background-color: rgba(0, 0, 0, 0.5);
    }

    .modal-content {
        background-color: #fefefe;
        margin: 15% auto;
        padding: 10px;
        border: 1px solid #888;
        width: 50%;
        max-width: 500px;
        border-radius: 5px;
    }

    .modal-header {
        padding: 15px 5px 3px 3px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        font-size: 22px;
        border-bottom: 1px solid black; /* Border chỉ ở phía dưới */
    }

    .modal-body {
        margin-top: 10px;
        margin-bottom: 10px;
        font-size: 19px;
    }

    .modal-footer {
        display: flex;
        justify-content: flex-end;
        align-items: center;
        padding: 15px;
    }

    .btn {
        margin-right: 10px; /* Khoảng cách giữa hai nút */
        width: 100px;
        height: 35px;
        font-size: 15px;
        border-radius: 10px;
        color: white;
        font-weight: bold;
    }

    a {
        text-decoration: none;
    }

    .btn-primary {
        background-color: #33FF00;
        display: flex;
        justify-content: center;
        align-items: center;
    }

        .btn-primary:hover {
            transition: background-color 0.3s ease;
            background-color: #66CC00;
        }

    .btn-secondary {
        background-color: #FF3366;
    }

   
</style>
